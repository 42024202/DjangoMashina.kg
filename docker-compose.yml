version: '3.9'

services:
  web:
    build:
      context: .
    container_name: mashina_web
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app:Z
    ports:
      - "8000:8000"
    env_file:
      - .env

